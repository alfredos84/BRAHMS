# ── Python ───────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
venv/
*.egg-info/

# ── Compiled engine binaries (built locally by the installers) ────────
engine_omp/twm_cpu
engine_omp/twm_cpu_*
engine_omp/bench_*
engine_omp/*.o
engine_gpu/twm
engine_gpu/twm.exe
engine_gpu/*.o
engine_gpu/*.obj
engine_gpu/*.lib
engine_gpu/*.exp
engine_gpu/*.pdb

# ── Windows GPU-engine dependencies (installed locally by setup_windows.ps1)
engine_gpu/vcpkg_installed/
engine_gpu/vcpkg_project/

# ── Runtime data (regenerated automatically) ───────────────────────────
crystals/*.db
crystals/*.db-shm
crystals/*.db-wal
output/
output_full/

# ── OS / editor cruft ───────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp

# ── Local-only, not part of the GUI repo ────────────────────────────────
BRAHMS_latex/
engine_omp/twm_cpu.exe
